import React from "react";
import "./App.css";

const LoginButton = () => {
    return (
        <div>
            <button className="Login">Login</button>
        </div>
    );
};

export default LoginButton;